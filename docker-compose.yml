services:
  pulseratings:
    build: .
    container_name: pulseratings-brasil
    ports:
      - "8080:80"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/Sao_Paulo
    restart: unless-stopped
